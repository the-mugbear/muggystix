# Supported Upload Formats

> **Last verified against:** backend 2.370.2 / frontend 5.248.1 (2026-09-19)

Upload is **staged**: choose files → review formats → import. BlueStick detects each file's format and shows how it was recognised — **by structure** (the content alone identifies it), **by filename only** (just a hint — you confirm or choose), or **not recognised** (a parser it would merely try). Only a structural match is ready to import without you. If detection picked the wrong tool, choose the format in the review: the file is then parsed as exactly that and nothing else, so a wrong choice fails visibly instead of silently routing elsewhere. Nothing is parsed until you press Import. The table below summarises the parser coverage and what your source tools need to export.

> **Maintainers / power users:** [`PARSERS.md`](./PARSERS.md) documents exactly **which fields each parser extracts** and where they land in the schema, plus a step-by-step **guide to adding a new parser**.

| Tool | Accepted Extensions | Parser Module | Notes |
| --- | --- | --- | --- |
| Nmap | `.xml` (normal), `.gnmap` (grepable), `.txt` (grepable) | `backend/app/parsers/nmap_parser.py`, `backend/app/parsers/gnmap_parser.py` | Use `-oX` for XML or `-oG` for GNMAP. Grepable output saved with a `.txt` extension is detected by its `Host: … Ports:` lines. Multi-host files are supported. |
| Masscan | `.xml`, `.json`, `.txt` list output | `backend/app/parsers/masscan_parser.py` | `-oX` XML (detected by `scanner="masscan"`), `-oJ` JSON (put `masscan` in the filename, or records with an `ip` + nested `ports` array), or `-oL` list output (`Timestamp: … Host: … Ports:` lines). |
| Naabu | `.json`, `.txt` | `backend/app/parsers/naabu_parser.py` | `host:port` text is recognised by its lines and JSON by its flat `{ip, port}` records (v2.353.0); `naabu` in the filename is only a hint. |
| RustScan | `.txt` | `backend/app/parsers/rustscan_parser.py` | Supports common console output with `Open <ip>:<port>` or bracketed port lists. Include `rustscan` in the filename. |
| Nessus | `.nessus`, `.xml` (recognised by its `NessusClientData_v2` structure) | `backend/app/parsers/nessus_parser.py`, `backend/app/services/nessus_integration_service.py` | Large (~600 MB) exports are streamed; ensure `scan-results.nessus` style XML, not HTML reports. |
| OpenVAS / Greenbone | `.xml` | `backend/app/parsers/openvas_parser.py` | Expects XML reports with `<result>` entries containing host/port/finding data. |
| Amass / Subfinder | `.json`, `.txt` | `backend/app/parsers/amass_parser.py` | Rows with resolved IPs create hosts and A/AAAA observations. Hostname-only rows become unresolved names in the Names inventory (no host, no invented IP) until a later upload resolves them. Text is recognised by hostname-per-line or `hostname ip` lines (v2.353.0). The tool recorded on the scan is the source tool named at import, else what the JSON shape says (`name` + `addresses` = amass, `input` + `source` = subfinder), else a filename hint; a plain list with none of these is recorded as `hostname-list`, not guessed. |
| Nikto | `.json`, `.csv`, `.txt` | `backend/app/parsers/nikto_parser.py` | Text reports should preserve the standard `Target IP` / `Target Port` header lines. |
| SMBMap | `.json`, `.txt` | `backend/app/parsers/smbmap_parser.py` | Text reports should preserve the standard smbmap `[+] <ip>` host lines. |
| BloodHound / SharpHound | `.json` | `backend/app/parsers/bloodhound_parser.py` | Upload extracted JSON files, not the ZIP bundle. Computer objects should include resolved IPv4 data. Files ≥50 MB are streamed via `ijson` to avoid OOM-killing the worker (v2.41.0). |
| Eyewitness | `.json`, `.csv`, `.zip` | `backend/app/parsers/eyewitness_parser.py` | Works with `--results` JSON bundle or the generated CSV summary. The CSV is recognised by its header (`Screenshot Path`, or `URL` + `Protocol` + `Port`) — `eyewitness`/`report` in the filename is only a hint (v2.353.0). ZIP bundles get decompression-bomb caps: ≤50 MB per file, ≤500 MB total, ≤5000 entries (v2.41.0). Skip counts + warnings persist on the ingestion job (v2.22.0). |
| httpx | `.json`, `.jsonl`, `.ndjson` | `backend/app/parsers/httpx_parser.py` | Project Discovery's `httpx` web probing — feeds the unified `web_interfaces` view alongside Eyewitness. Skip counts + warnings persist on the ingestion job. |
| whatweb | `.json`, `.jsonl` | `backend/app/parsers/whatweb_parser.py` | WhatWeb's `--log-json` web tech-fingerprint — feeds the unified `web_interfaces` view (`source="whatweb"`) with title / server header / detected tech stack. The apt-installable alternative when httpx (Go binary / Python-CLI collision) isn't available. No favicon hash or structured TLS (whatweb doesn't emit them). Example invocation: `whatweb -a 3 --input-file=targets.txt --log-json=whatweb.json --no-errors`. |
| testssl.sh | `.json` | `backend/app/parsers/testssl_parser.py` | testssl.sh TLS assessment (`--jsonfile` / `--jsonfile-pretty`). Folds the per-check finding array into one `web_interfaces` row per `(ip, port)` (`source="testssl"`), promoting the weak-TLS protocol flag (`tls_weak_protocol` — SSLv2/SSLv3/TLS 1.0/1.1 offered → the `weak_tls` systemic condition + `has:weak_tls`) plus cert expiry / self-signed state. Example: `testssl --jsonfile out.json https://target:443`. |
| DirBuster / Gobuster / Feroxbuster / ffuf / Dirsearch | `.json`, `.csv`, `.txt` | `backend/app/parsers/dirbuster_parser.py` | Unified parser for directory brute-force tools. ffuf / feroxbuster / dirsearch JSON is recognised by its shape and gobuster text by its `(Status: NNN)` lines; a tool name in the filename is only a hint. |
| NetExec (NXC) | `.json`, `.txt` | `backend/app/parsers/netexec_parser.py` | Text output: the SMB and LDAP enumeration lines (SMB signing posture is recorded from the SMB line). JSON: `spider_plus` module output. Recognised by the SMB / LDAP column layout of the console text; `netexec` or `nxc` in the filename is only a hint. |
| DNS inventories | `.csv` | `backend/app/parsers/dns_parser.py` | Expect columns like `hostname`, `record_type`, `value`. DNS records come from uploaded files only — the server never resolves anything itself (run `dnsx`/`dig` locally and upload the output). |
| dnsx | `.json`, `.jsonl`, `.ndjson` | `backend/app/parsers/dnsx_parser.py` | Project Discovery's `dnsx` resolver — run terminal-side against operator-supplied DNS servers, upload the JSON output to ingest A/AAAA/CNAME/MX/NS/TXT/SOA/PTR records. PTR answers feed `Host.hostname`. Resolver attribution + resolution-failure tallies (NXDOMAIN, SERVFAIL) surface as parser warnings on the ingestion job. Example invocation: `dnsx -j -resp -l ips.txt -r resolvers.txt -ptr -a -aaaa -cname -mx -ns -txt -o dnsx-output.json`. |
| RDAP | `.json`, `.ndjson` | `backend/app/parsers/rdap_parser.py` | Network-registration provenance — who a netblock is registered to (org, country, registry handle) and, where the source supplies it, the ASN. Generate with the bundled `./scripts/rdap-lookup.py` (stdlib only, no pip install), which resolves the authoritative RDAP server per prefix via the IANA bootstrap registry, deduplicates **per netblock** rather than per host, and skips private/reserved space. Workflow: export **Reports → Host Inventory (CSV)**, then `./scripts/rdap-lookup.py --input hosts.csv --output rdap.ndjson` (the export's `IP Address` column is read on its own, so Subnet CIDRs and service banners don't become lookups), then upload the result. Ingest attaches each host to the block covering it, queryable as `org:` / `asn:` in the host DSL (`cloud:` arrives with the prefix-list importer). Egress deliberately stays operator-side — the backend makes no registry calls, so air-gapped deployments work and registry rate limits never touch a web request. |
| Subnet lists | `.csv` | `backend/app/parsers/subnet_parser.py` | Scope import (Scopes page): CIDR rows AND domain / wildcard rows, with labels, description and site metadata. Domain scope never confers subnet scope. |

## File Size & Performance

- Single uploads are capped by `MAX_FILE_SIZE` (default 1 GB) and streamed to disk, so browser timeouts are avoided.
- Upload requests return quickly with an ingestion job ID; once started, parsing continues in a background worker and the UI follows the job.
- **An identical file is refused.** Every upload is hashed (SHA-256); a file already in the project — as a scan, still parsing, or staged awaiting its format review — answers `409 duplicate_scan` naming what it already is. Re-importing on purpose is an explicit choice ("Import again" in the review; *Re-process* on Ingestion Results).
- **Files are retained for `INGESTION_RETAIN_FILES_DAYS` (default 7) after a job finishes**, then only the bytes are removed — the job row stays. Retry-with-a-different-format and Re-process work only inside that window. A staged file nobody starts expires after 24 hours.
- The review stages 4 files at a time and starts 6 at a time, however many you select.
- Nessus files commit in batches (`NESSUS_COMMIT_BATCH_SIZE`, default 50 hosts) to keep database pressure manageable.
- **Parser quality stats** (v2.22.0) — parsers that drop records report `skipped_count` and a free-text `parser_warnings` string on the ingestion job row. Visible on the Scans / Ingestion Jobs page; useful for spotting silent data loss in malformed inputs.

## Quick Tips

- Compressing files before upload is not required; submit raw exports so the auto-detection logic can inspect headers.
- If a file fails to parse, open **Inventory → Ingestion Results** (the *Needs attention* filter) for the structured error, the format chain (*detected → override → final*) and, while the file is retained, *Review the format and retry*.
- When migrating a deployment, keep `.env` aligned with the host IP and CORS origins.
